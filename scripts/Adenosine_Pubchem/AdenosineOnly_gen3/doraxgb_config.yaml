input:
  reactionCSV: "reactionDF.csv"

output:
  fileNamePrefix: "AdenosineOnly_gen3"

scoring:
  testMode: false
  testRows: 100

  useMultiprocessing: true
  max_cores: 8
  mpChunkSize: 64
  cacheSize: 200000

  # STRICT pinning (recommended on shared servers)
  # choose exact logical CPU IDs on your machine:
  #core_ids: [0, 1, 2, 3, 4, 5, 6, 7]
